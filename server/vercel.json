{
  "version": 2,
  "buildCommand": "yarn build",
  "installCommand": "yarn install --production=false",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
